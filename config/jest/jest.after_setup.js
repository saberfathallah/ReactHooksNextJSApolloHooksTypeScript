// let consoleErrorSpy;


// import * as cookies from "@services/cookies";

global.beforeEach(() => {
  // cookies.buildCookies = jest.fn().mockReturnValue({
  //   get: () => ({
  //     token: "toekn",
  //   }),
  // });
});

global.afterEach(() => {
  jest.clearAllMocks();

});
